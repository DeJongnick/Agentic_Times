# input user_request
# output plan détaillé

class PlanWriter:
    def __init__(self, model=None):
        self.model = model  # éventuel modèle de génération de plan

    def write_plan(self, user_request):
        """
        Génère un plan détaillé à partir de la requête utilisateur.
        Args:
            user_request (str): La demande de l'utilisateur.
        Returns:
            str: Le plan détaillé généré.
        """
        # À implémenter : logiques de génération de plan
        pass